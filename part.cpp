#include<iostream>

struct Particle {
    float x, y;
    float vx, vy;
    float ax, ay;
};

const int WIDTH = 80;
const int HEIGHT = 20;

const float GRAVITY = 0.1f;

const float DELTA_TIME = 0.1f;

void clearScreen() {
    std::cout << 
}